---
description: how to automatically push changes to GitHub
---

Whenever a task is completed and verified, you MUST push the changes to GitHub.

// turbo-all
1. Stage all changes: `git add .`
2. Commit changes with a descriptive message: `git commit -m "[Descriptive Message]"`
3. Push to the remote repository: `git push origin main`
